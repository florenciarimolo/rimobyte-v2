---
import Button from '../ui/Button.astro';
---
<header id="navbar" class="fixed top-0 left-0 right-0 z-50 py-5 transition-all duration-300 border-b border-transparent">
  <div class="max-w-[1200px] mx-auto px-[clamp(1.5rem,5vw,4rem)] flex items-center justify-between">

    <a href="/" class="font-display font-bold text-[1.375rem] tracking-[-0.03em] no-underline">
      <span class="text-white">Rimo</span><span class="text-gradient-logo">Byte</span>
    </a>

    <nav class="hidden md:flex items-center gap-8" aria-label="Principal">
      {(['Proyectos', 'Servicios', 'Sobre mí'] as const).map(item => (
        <a
          href={`/${item.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, '').replace(/\s/g, '-')}`}
          class="font-body text-sm text-text-secondary no-underline hover:text-white transition-colors duration-200"
        >{item}</a>
      ))}
    </nav>

    <Button href="#contacto" variant="primary" class="hidden md:inline-flex">Empezar mi web</Button>

    <button
      id="menu-btn"
      type="button"
      class="md:hidden flex flex-col gap-1.5 p-2 bg-transparent border-0 cursor-pointer"
      aria-label="Abrir menú"
      aria-expanded="false"
      aria-controls="mobile-menu"
    >
      <span class="block w-6 h-px bg-white transition-all duration-200"></span>
      <span class="block w-6 h-px bg-white transition-all duration-200"></span>
      <span class="block w-4 h-px bg-white transition-all duration-200"></span>
    </button>
  </div>
</header>

<div
  id="mobile-menu"
  class="fixed inset-0 z-40 bg-bg-base flex flex-col items-center justify-center gap-8 translate-x-full transition-transform duration-300"
  hidden
  inert
>
  {['Proyectos', 'Servicios', 'Sobre mí', 'Contacto'].map(item => (
    <a href={`/${item.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, '').replace(/\s/g, '-')}`}
       class="font-display text-3xl font-bold text-white no-underline">{item}</a>
  ))}
</div>

<script>
  const navbar = document.getElementById('navbar')!;
  const menuBtn = document.getElementById('menu-btn') as HTMLButtonElement;
  const mobileMenu = document.getElementById('mobile-menu') as HTMLElement;
  let menuOpen = false;

  function setMenuOpen(open: boolean) {
    menuOpen = open;
    mobileMenu.hidden = !open;
    mobileMenu.inert = !open;
    mobileMenu.style.transform = open ? 'translateX(0)' : 'translateX(100%)';
    menuBtn.setAttribute('aria-expanded', String(open));
    menuBtn.setAttribute('aria-label', open ? 'Cerrar menú' : 'Abrir menú');
    if (open) {
      const firstLink = mobileMenu.querySelector('a') as HTMLAnchorElement | null;
      firstLink?.focus();
    }
  }

  function syncNavHeight() {
    const h = navbar.offsetHeight;
    if (h > 0) {
      document.documentElement.style.setProperty('--nav-height', `${h}px`);
    }
  }
  syncNavHeight();
  window.addEventListener('resize', syncNavHeight, { passive: true });
  requestAnimationFrame(syncNavHeight);

  window.addEventListener('scroll', () => {
    const scrolled = window.scrollY > 40;
    navbar.style.background = scrolled ? 'rgba(10,10,18,0.8)' : 'transparent';
    navbar.style.backdropFilter = scrolled ? 'blur(16px)' : 'none';
    navbar.style.borderBottomColor = scrolled ? 'rgba(255,255,255,0.07)' : 'transparent';
  });

  menuBtn.addEventListener('click', () => setMenuOpen(!menuOpen));

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && menuOpen) setMenuOpen(false);
  });

  mobileMenu.querySelectorAll('a').forEach(a => {
    a.addEventListener('click', () => setMenuOpen(false));
  });
</script>
