---
import Base from '../../layouts/Base.astro';
import Navbar from '../../components/sections/Navbar.astro';
import HeroServicio from '../../components/sections/servicios/HeroServicio.astro';
import ParaQuienEs from '../../components/sections/servicios/ParaQuienEs.astro';
import QueIncluye from '../../components/sections/servicios/QueIncluye.astro';
import PrecioServicio from '../../components/sections/servicios/PrecioServicio.astro';
import CasosRelacionados from '../../components/sections/servicios/CasosRelacionados.astro';
import OtrosServicios from '../../components/sections/servicios/OtrosServicios.astro';
import CTAFinalServicio from '../../components/sections/servicios/CTAFinalServicio.astro';
import ContactForm from '../../components/sections/ContactForm.tsx';
import FAQ from '../../components/sections/FAQ.tsx';
import Footer from '../../components/sections/Footer.astro';
import { services } from '../../data/services';

const service = services.find((s) => s.slug === 'mantenimiento-web')!;
const recaptchaSiteKey = import.meta.env.RECAPTCHA_SITE_KEY ?? '';

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: service.faqs.map((faq) => ({
    '@type': 'Question',
    name: faq.question,
    acceptedAnswer: { '@type': 'Answer', text: faq.answer },
  })),
};

const serviceSchema = {
  '@context': 'https://schema.org',
  '@type': 'Service',
  name: 'Mantenimiento web para negocios',
  provider: { '@type': 'Person', name: 'Flor Rímolo', url: 'https://rimobyte.com' },
  areaServed: { '@type': 'Country', name: 'España' },
  description: service.seo.description,
  offers: {
    '@type': 'Offer',
    priceCurrency: 'EUR',
    price: '50',
    priceSpecification: { '@type': 'PriceSpecification', minPrice: '50', priceCurrency: 'EUR' },
  },
  url: 'https://rimobyte.com/servicios/mantenimiento-web',
};
---

<Base title={service.seo.title} description={service.seo.description}>
  <script slot="head" is:inline type="application/ld+json" set:html={JSON.stringify(faqSchema)} />
  <script slot="head" is:inline type="application/ld+json" set:html={JSON.stringify(serviceSchema)} />

  <Navbar />
  <main id="main">
    <HeroServicio service={service} />
    <ParaQuienEs
      points={service.targetAudience}
      eyebrowText={service.targetAudienceEyebrow}
      title={service.targetAudienceTitle}
      titleEm={service.targetAudienceTitleEm}
      footnote={service.targetAudienceFootnote}
      countLabel={service.targetAudienceCountLabel}
    />
    <QueIncluye features={service.features} />
    <PrecioServicio price={service.price} note={service.priceNote} />
    <CasosRelacionados
      cases={service.relatedCases}
      title={service.relatedCasesTitle}
      titleEm={service.relatedCasesTitleEm}
    />
    <FAQ faqs={service.faqs} client:load />
    <OtrosServicios currentSlug={service.slug} />
    <CTAFinalServicio
      title={service.ctaFinal.title}
      titleEm={service.ctaFinal.titleEm}
      text={service.ctaFinal.text}
    />
    <ContactForm
      client:load
      recaptchaSiteKey={recaptchaSiteKey}
      origin="mantenimiento-web"
      omitAnchor
    />
  </main>
  <Footer />
</Base>
